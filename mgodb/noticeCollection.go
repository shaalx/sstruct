package mgoDB

// save notice to mongodb,and send an e-mail
func SaveNotice(data interface{}) bool {
	// noticeDB := SetDB(DBnoticeServer...)
	// defer noticeDB.Close()
	// mail.SendMail(data)
	// return noticeDB.Save(data)
	return true
}
